# Local config
# [[ -f ~/.zshrc.local ]] && source ~/.zshrc.local

# aliases
[[ -f ~/.aliases ]] && source ~/.aliases

#export PATH="$PATH:$(yarn global bin)"
export PATH="$PATH:$HOME/.yarn/bin:$HOME/.local/bin:$HOME/.cargo/bin"
export GITHUBSSH="ssh://git@ssh.github.com:443/"
